[package]
name = "privachain_dr_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
privachain_crypto = { path = ".." }
uniffi = "0.28"
bincode = "1.3"
lazy_static = "1.5"

[build-dependencies]
uniffi = { version = "0.28", features = ["build"] }
