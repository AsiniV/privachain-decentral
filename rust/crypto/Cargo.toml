[package]
name = "privachain_crypto"
version = "0.1.0"
edition = "2021"

[dependencies]
privachain_messenger = { path = "../../messenger" }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"

[lib]
crate-type = ["rlib"]
