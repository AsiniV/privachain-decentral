# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*-dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
.env.production
.env.staging
*.env
!.env.template
!.env.example
**/agent-eval-report*
packages
!packages/resolver/
.file-manifest
.devcontainer/

# Rust build artifacts
target/
Cargo.lock

# Contract build artifacts
contracts/*/target/
contracts/mail/target/
contracts/artifacts/*.wasm

# ZK circuit build artifacts (v4.0)
build/zk/

# Gecko engine binaries (downloaded/built separately)
# Exclude actual firefox binary but keep placeholder and docs
src-tauri/binaries/gecko-slim/firefox
src-tauri/binaries/gecko-slim/browser/*
!src-tauri/binaries/gecko-slim/*.placeholder
!src-tauri/binaries/gecko-slim/*.md
circuits/**/*.r1cs
circuits/**/*.wasm
circuits/**/*.sym
circuits/**/*.zkey
circuits/**/*.ptau
circuits/**/verification_key.json
circuits/**/verifier.sol

# Tauri generated files
src-tauri/gen/

# Temporary files
/tmp/
*.tmp
*.temp

# IPFS output files
ipfs/out/
*.car

# Backup files
*.backup
*_old.*

# OrbitDB files and directories
orbitdb/
.orbitdb/
*.orbitdb/

# Test directories that should be excluded from builds but not from git
# (tests/ folder is kept in git for actual test files)
actionlint
cosmos/contract/*.wasm

# Local databases
*.db
vault.db
